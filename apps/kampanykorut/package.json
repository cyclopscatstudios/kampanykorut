{
  "name": "kampanykorut",
  "private": true,
  "version": "0.1.0-alpha",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "playwright": "playwright test",
    "playwright:headed": "playwright test --ui --headed",
    "test-ct": "playwright test -c playwright-ct.config.ts",
    "playwright:ct": "playwright test --config=playwright-ct.config.ts --ui"
  }
}
